[pytest]
testpaths = examples use_cases recipes
asyncio_mode = auto
python_files = test_*.py
python_functions = test_*
addopts = -v --tb=short
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    integration: marks tests that require real API access

